# 09 — Users & Groups

## Commands Learned
whoami
id
groups

cat /etc/passwd
cat /etc/group

## User Management
sudo adduser newuser
sudo passwd newuser
sudo deluser olduser
usermod -aG sudo username

## Examples Practiced
adduser testuser
groups testuser

## Summary
I can inspect, manage, and modify users and groups like a system admin.
