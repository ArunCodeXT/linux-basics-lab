# 06 — Searching

## Commands Learned
grep "text" file
grep -i "text" file → ignore case
grep -r "text" folder → recursive

find /path -name "*.txt"
find . -type f -size +1M

locate filename
which python

## Examples Practiced
grep -i "error" /var/log/syslog
find ~/Documents -name "*.pdf"

## Summary
I can search files, folders, and content anywhere in Linux accurately.
